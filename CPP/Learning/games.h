#ifndef GAMES_H
#define GAMES_H

#include <iostream>
#include <random>
#include <string>
using std::cout;
using std::endl;
using std::cin;
using std::string;
using std::stoi;

int main(int argc, char const *argv[]);
void GuessingGame();
void DicePoker();
// int countNumbers(string& s);
// int* splitInput(string& s, int count);

#endif